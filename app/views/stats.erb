<div class="profile">
<h1>stats</h1>	
<h2>For Survey: <%= @survey.name %></h2>
<h4>Survey was completed <%= @completed_times %> times. </h3>
<ol>Questions:<br>
	<% @questions.each do |question| %>
		<li><%= question.text %><br>
			<% question.choices.each do |choice| %>
					<%= choice.choice %>
					chosen <%= choice.responses.count %> times.<br>
			<% end %></li>
	<% end %>
	</ol>
</div>
