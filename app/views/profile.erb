<h1>Profile</h1>
<div><%= current_user.name  %></div> 


<h3>Surveys</h3>

<table id='survey_table'>
  <ul>
    <thead>
      <th>Survery Name</th>
      <th>Results</th>
    </thead>
    <tbody>
      <% if @created %>
      <% @created.each do |survey| %>
      <tr>
        <td>  <%=survey.name%> </td>
        <td> <a href="/survey/#{survey.id}/results"> Results </a></td>
      </tr>
      <% end %>
  </ul>
  <% end %>
  </tbdoy>
</table>

<br>
<div>
<a id='new_survey' href="/survey/new">New Survey</a>
</div>

